
import React from "react";

const EditProfile = () => {

    // should already have old data from the component before as well as the structure

    const userInfo = "this is where the user info object is stored and then can be further manipulated"
    
    //need to target the fields 


//   return;
};

export default EditProfile;
